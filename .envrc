export TYPST_ROOT="$PWD"
# workaround: global shell profile exports a mis-escaped TYPST_PACKAGE_PATH
# (literal backslash-space) that breaks @local package resolution
unset TYPST_PACKAGE_PATH
PATH_add bin
source_env_if_exists .envrc.local
