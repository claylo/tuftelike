# First Chapter

This is the first chapter's opening paragraph. It exists to verify that
`begin-chapters` correctly starts heading numbering once the numbering
set-rule is installed via the `#show:` chain rather than a bare call.

A second paragraph, so the chapter has some visible body before the next
chapter break.
