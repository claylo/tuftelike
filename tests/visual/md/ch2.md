# Second Chapter

This is the second chapter. It should land on a recto page in print media,
with a blank verso inserted before it if needed, because `chapters()` joins
chapter files with a `pagebreak(to: "odd")` separator.
